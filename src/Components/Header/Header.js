import React from 'react';
import TitleBar from '../TitleBar/TitleBar';

const Header = () => {
    return (
        <div>
            <TitleBar></TitleBar>
        </div>
    );
};

export default Header;